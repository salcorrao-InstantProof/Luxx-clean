import {error} from '../lib/http.mjs';import {verifySession} from '../lib/auth.mjs';import {store} from '../lib/storage.mjs';import {STORES} from '../lib/model.mjs';
export default async req=>{if(!verifySession(req))return error('Unauthorized',401);const key=new URL(req.url).searchParams.get('key')||'';if(!/^(original|variant|hls|thumb)\/[A-Za-z0-9_.\/-]+$/.test(key)||key.includes('..'))return error('Invalid media key');const s=await store(STORES.media);const x=await s.getWithMetadata(key,{type:'arrayBuffer'});if(!x)return error('Not found',404);const ct=x.metadata?.contentType||'application/octet-stream';return new Response(x.data,{headers:{'content-type':ct,'cache-control':'private, max-age=3600','content-length':String(x.data.byteLength)}});};export const config={path:'/api/media-chunk'};
