import {json,error} from '../lib/http.mjs';import {verifySession} from '../lib/auth.mjs';import {store} from '../lib/storage.mjs';import {STORES} from '../lib/model.mjs';
export default async req=>{if(!verifySession(req))return error('Unauthorized',401);const u=new URL(req.url),asset=(u.searchParams.get('asset')||'').replace(/[^A-Za-z0-9_.-]/g,''),variant=(u.searchParams.get('variant')||'').replace(/[^A-Za-z0-9_.-]/g,'');if(!asset)return error('Invalid request');const s=await store(STORES.assets),a=await s.get(`assets/${asset}.json`,{type:'json'});if(!a)return error('Asset not found',404);if(!variant){const keys=Array.from({length:a.parts},(_,i)=>`original/${asset}/part-${String(i).padStart(6,'0')}`);return json({ok:true,name:a.name,type:a.type||'application/octet-stream',sha256:a.master_sha256,parts:keys.map(key=>({key,url:`/api/media-chunk?key=${encodeURIComponent(key)}`}))});}const v=(a.variants||[]).find(x=>x.variant_id===variant);if(!v?.download_parts)return error('Download not ready',404);return json({ok:true,name:v.output_name,type:v.variant_type==='IMAGE_EDIT'?'image/jpeg':'video/mp4',sha256:v.sha256,parts:v.download_parts.map(key=>({key,url:`/api/media-chunk?key=${encodeURIComponent(key)}`}))});};export const config={path:'/api/download-manifest'};
